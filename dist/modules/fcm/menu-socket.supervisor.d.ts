import { OnModuleDestroy, OnModuleInit } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { EnsHttpService } from '../../infrastructure/ens-backend/ens-http.service';
import { PrismaService } from '../../infrastructure/prisma/prisma.service';
import { ExpressJwtRelayService } from './express-jwt-relay.service';
import { FcmDeviceService } from './fcm-device.service';
import { FcmSenderService } from './fcm-sender.service';
import { MenuSocketLockService } from './menu-socket.lock';
export declare class MenuSocketSupervisor implements OnModuleInit, OnModuleDestroy {
    private readonly config;
    private readonly prisma;
    private readonly devices;
    private readonly locks;
    private readonly jwtRelay;
    private readonly sender;
    private readonly ensHttp;
    private readonly logger;
    private readonly relays;
    private reconcileTimer;
    private listenClient;
    private shuttingDown;
    private draining;
    reconnectsTotal: number;
    constructor(config: ConfigService, prisma: PrismaService, devices: FcmDeviceService, locks: MenuSocketLockService, jwtRelay: ExpressJwtRelayService, sender: FcmSenderService, ensHttp: EnsHttpService);
    get desiredMenus(): number;
    get joinedMenus(): number;
    get uncoveredMenus(): number[];
    onModuleInit(): Promise<void>;
    onModuleDestroy(): Promise<void>;
    private startListen;
    reconcileAll(): Promise<void>;
    reconcileMenu(menuId: number): Promise<void>;
    private getOrCreate;
    private acquire;
    private connectRelay;
    private attachListeners;
    private scheduleReconnect;
    private teardown;
    private handleNewCall;
    private handleChanged;
    private enrichmentCache;
    private resolveChannel;
    private catchUpPending;
}
